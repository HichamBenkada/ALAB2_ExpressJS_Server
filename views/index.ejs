<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Server Side Rendering |<%=title%> </title>
    <link rel="stylesheet" href="/style.css">
  </head>
  <body>
   <%- include('./partials/header.html') %>
    <div id="content">
      <% options.forEach((option)=>{ %>
      <!-- we use equal "=" to get the assigned value -->
      <h2> <%= option.title %></h2>
      <!-- we use dush "-" to include html -->
      <p> <%- option.content %> </p>
      <%})%>
    </div>

    <%- include('./partials/footer.html') %>
  </body>
</html>
